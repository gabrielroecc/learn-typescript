const annotation:string = "Hello, World!" // done with annotation
let inference = "Hello, World!" // done with Inference

//with inference and annotation, TS let us change the variable with only datas that have the same type
